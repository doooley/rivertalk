Chat attempt.

compile connect.c as follows - (assuming ubuntu or equiv.)
gcc -o ipaddress connect.c

to run
./ipaddress www.example.com
or whatever.

START SCREEN
to compile run the following
gcc -o startwlog startscreen.c -lncurses 
